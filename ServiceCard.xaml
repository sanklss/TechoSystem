<UserControl x:Class="TechnoSystem.ServiceCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TechnoSystem"
             xmlns:converter="clr-namespace:TechnoSystem.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="160" d:DesignWidth="500"
             Margin="5">
             
    <UserControl.Resources>
        <converter:IntToVisibilityConverter x:Key ="IntToVisibilityConverter"/>
    </UserControl.Resources>
    <Border BorderBrush="Green" Background="{Binding backGroundColor}" BorderThickness="3">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Image Source = "{Binding FullImagePath}" Height="110"/>
                <Label Content="Количество доступных лицензий: "/>
                <Label Content="{Binding FreeLicense}"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Label Content="Название: "/>
                <Label Content="{Binding Name}" FontWeight="{Binding TitleFontWeight}"/>
                <Label Content="Тип услуги: "/>
                <Label Content="{Binding Service.Name}"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <Label Content="Срок подписки: "/>
                <Label Content="{Binding MonthCount}"/>
                <Label Content="Дата: "/>
                <Label Content="{Binding Date}"/>
                <Label Content="Цена: "/>
                <Label Content="{Binding Price}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
